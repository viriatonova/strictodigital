import MenuButton from "./MenuButton";
import MenuPrincipal from "./MenuPrincipal";

const NavPrincipal = () => {
    return (
        <nav>
            <MenuButton />
            <MenuPrincipal />
        </nav>
    );
};

export default NavPrincipal;
