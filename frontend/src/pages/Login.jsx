import LoginForm from "../components/forms/LoginForm";

const Login = () => {
    return (
        <main className="login">
            <div>
                <LoginForm />
            </div>
        </main>
    );
};

export default Login;
